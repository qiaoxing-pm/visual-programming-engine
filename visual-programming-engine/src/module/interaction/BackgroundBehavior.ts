/**
 * Backward-compatible re-export. Prefer `CanvasHostBackground` from `../canvas-background`
 * when copying this code into other repositories.
 */
export { CanvasHostBackground as default } from "../canvas-background/index.js";
export type { CanvasHostBackgroundOptions as BackgroundBehaviorOptions } from "../canvas-background/index.js";
